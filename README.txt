Проект спринт 7

Тестирование АPI

тесты запускаются в командной строке: pytest -v ... путь до теста

для формирования отчета Alure выполните команду: pytest <имя файла с тестами> --alluredir=allure_results
после чего сформировать отчет командой: allure serve allure_results

